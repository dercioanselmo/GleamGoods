name: CI - Cart

on:
  push:
    branches: [main]
    paths: [cart/**]
    tags: ["cart/v*"]

permissions:
  id-token: write
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/ci-build-push.yml
    with:
      service: cart
      ecr_repository: gleamgoods/cart
    secrets: inherit
